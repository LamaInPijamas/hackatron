<%= form_with(model: data_upload) do |form| %>
  <% if data_upload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(data_upload.errors.count, "error") %> prohibited this data_upload from being saved:</h2>

      <ul>
        <% data_upload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file_type, "Typ pliku" %>
    <%= form.select :file_type, options_for_select([['Obiazenie', 'load'], ['Cena i PV', 'price_panel']]) %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :file, style: "display: block" %>
    <%= form.file_field :file %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

